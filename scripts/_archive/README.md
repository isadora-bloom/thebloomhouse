# Archived one-shot scripts

Spent historical migration appliers (`apply-wave*.mjs`), moved out of the
active `scripts/` dir so they can't be run accidentally. They applied
specific migrations that are long since live; kept for reference only.
Not wired into anything. (HARDENING-SCOPE Area 2 Tier B.)
