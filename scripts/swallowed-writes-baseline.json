{
  "count": 0,
  "note": "Un-wrapped statement-level awaited insert/upsert in src/ (HARDENING-SCOPE Area 1). COMPLETE: 123 → 120 → 100 → 0. Every such write now routes through writeOrLog (src/lib/db/write-or-log.ts) — a failed write is logged (db.write_failed), never silently dropped. Ratchet stays at 0: any NEW swallowed write fails CI. If a legitimate new pattern needs an exception, justify it — do not raise this number."
}
