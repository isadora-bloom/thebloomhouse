# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/

# env files (NEVER commit real keys)
.env
.env.*
!.env.local.example
!.env.example
!.env.test.example

# debug
npm-debug.log*

# misc
.DS_Store
*.tsbuildinfo
next-env.d.ts

.vercel
.claude/*
!.claude/settings.json

# Phase 2 operator-data exports (PII; never commit)
phase2-exports/

# Playwright journey screenshots (per run)
e2e/screens/

# Playwright output. The JSON report used to serialise the whole webServer env
# (every branch secret) and was tracked; never again.
e2e/results.json
e2e/report/
test-results/
