# 2026-09-18 · Isadora

## Done
- Migration 418 (portal calm) ran on production this morning: section release dates, the venue soft close, "where things stand", venue notes, timeline surprises, guest RSVP for a whole party. Verified afterwards, the count of soft-close triggers and policies matched the file.
- Ported the Rixey portal's vendor name matching and contact parsing into `vendor_bloom` as TypeScript (`lib/vendors/names.ts`, `contact-bits.ts`, `phone.ts`, commit `76d3af8`). Same rule as at Rixey: two names are one vendor only when the difference is bookkeeping, anything else is a question for a person, and a ruling beats the heuristics. The one change is that rulings are passed in rather than hard-coded, because on the network they are rows. Forty tests, built from the real cases: Sammy's nine spellings, Carpe Donut and Rodeo, ImTheDJ and Nate Clancy, Bride & Joy, the fax number that nearly became the number to ring.
- The vendor network database is live. Isadora sent the database password, the January draft came off, and `001` went on: 25 tables, row-level security on every one, the seed lists in (16 categories, 101 style tags, 32 specialties). `002` added the two private storage buckets. The Supabase CLI can't reach this older project (it fails making its login role), and `db query` only takes one statement anyway, so `scripts/run-sql.mjs` sends a whole file through `pg` as one query, which rolls back as a unit if anything fails.
- Built the venue side of the vendor network in `vendor_bloom` (`DESIGN.md` step 4, one commit after `ddfb564`). Sign in, overview, the vendor list with "our vendors / recommended / worked here / whole network", a vendor page split into what the vendor says about themselves (shared), their contact details (seen by every venue's staff, never couples), every contact detail ever seen and where it came from, bookings at this venue with contracts, and the venue's own private view. Adding a vendor checks the network first: "sammys rental inc." reuses Sammy's Rentals, "Genesis" is asked about against Genesis Catering, and only a person answers. Unplaced bookings get the same suggestions. Walked through with Playwright at desktop and phone width, every flow exercised: a booking's fax number stayed off the record, the couple-style contact text was kept as evidence rather than written on, the ruling made "Carpe Donut/Rodeo Catering" match Carpe Donut without asking.
- Checked whether the Bloom portal has the two faults that hid a Rixey couple's wedding party this week, and it has both. The website builder told couples "N active" by counting switches, while ten of the eleven sections on the published site return nothing when they have no content, and the builder's own preview filled the empty ones with encouragement ("Your love story will appear here...") for sections the live site drops. So a couple could switch a section on, see it in the preview with a heading, publish, and find nothing there. The section rules now live in `src/lib/website-sections.ts`, read by both the builder and `/w/[slug]`, and each one is pinned by a test to the guard it mirrors in the page. Eighteen tests. The panel now leads with what a guest can actually see, each section says whether it is showing, ready, on-but-empty or hidden and what to add, and the preview shows only what will render.
- Fixed a blank band on every published wedding website. The renderer's guard was `if (!content) return null`, and `content` is a JSX element, so it is always truthy: it only ever caught the default branch. An enabled-but-empty section rendered its padded `<section>` wrapper around nothing, between two real sections. It now asks the shared rules the same question the component is about to ask itself.

- The vendor's side of the network (`DESIGN.md` step 5). A vendor gets one private link, hashed in the database and good for six months, and behind it a page that is theirs: what they do, photos, how to reach them, pricing bracket and any offer, where they work, insurance and backup plan, who they welcome, styles and languages. Saving is live, there is no queue, and a blank answer stays blank. On the venue's vendor page there is now a "Their link" card: email it, or take a link to copy. Walked through with Playwright: link issued, profile written with a logo and a photo, a photo removed and really gone from storage, a made-up token gets the "link has run out" page, and the venue sees the finished profile.

- The couple directory (step 6). A server route hands Bloom's couple portal a venue's recommended vendors shaped for couples, behind a key, since couples have no login here and the list says who recommends whom. What a couple may see is one small function with a test on it: name, what they do, the vendor's own words and photos, website and socials, the venue's line; never email, phone, who to ask for, private notes, ratings or standing. A profile the venue hides keeps its listing and loses the vendor's words. The venue has a "What couples see" page showing the same output.
- Built the couple portal's audit trail, and measured the gap across the whole system first rather than just the portal. `scripts/audit-activity-logging.mjs` scopes by table, not by route: any handler writing a table with a `venue_id` or `wedding_id` is in scope, whoever calls it. It found **103 of 106 mutating API routes writing no activity entry at all** — admin 32, agent 22, intel 13, portal 10, couple 6 — on top of the 126 browser writes that never reach a route. It is wired into `npm run check:governance` as `check:activity-log` at a ceiling of 100, so the number can only fall.
- The mechanism: one route, `/api/couple/[resource]`, reading a 24-entry table in `src/lib/api/couple-resources.ts`. POST creates or upserts a whole-form resource, PATCH and DELETE take an id. Every write gets three things the browser writes never had: scope from the session rather than the payload (a body naming its own `wedding_id` is refused), a field whitelist that says out loud what it refused instead of dropping it, and an entry in `activity_log`. RLS is untouched and still correct; it is the second line now rather than the only one. `src/lib/api/couple-client.ts` mirrors supabase's `{ data, error }` shape so migrating a page is a swap, not a rewrite.
- Migrated the website builder to it, since that is the save that could not be traced. A failed save is now visible to the couple: it used to go through `writeOrLog`, which put a line in a server log nobody reads and let the button return to "Save Changes" as though it had worked.
- Logged the three highest-value routes outside the portal: the GDPR pair (`couple/me/erase`, `couple/me/export`) because an erasure request has a legal clock on it and a data export is everything a couple has leaving the building, and `portal/config/copy` because bulk config arriving from a sister venue is additive, indistinguishable from typed-in work afterwards, and feeds the correlation engine.

- Rixey's vendors are on the network (step 7). A dry run first, then the real thing: 227 vendors with the 49 merged-away rows kept and pointing at their survivors, Rixey's own view of all 178 live ones (116 recommended, 76 couple lines, 93 private notes), 303 bookings with 55 still unplaced as they were at Rixey, 125 contract files and 37 photos copied across, 238 contact-evidence rows with Rixey's provenance, and the 52 answered merge questions turned into 77 ruled names so nothing gets asked twice. Rixey's row ids were kept, so running it again changes nothing, which was checked. No vendor was emailed. Then walked the screens on the real data: Sammy's shows 23 bookings and 7 contracts that download; the unplaced page now offers a confident match on 19 of the 55, a maybe on 5, and flags 7 as not really names.

- Merge questions for Bloom staff (step 8), the last of the build order. A venue that isn't sure whether a near match is the same business clicks "ask Bloom" and carries on; the record is created and the question filed. Bloom staff see both records side by side and answer: same business, and the fuller one takes everything from the other with nothing deleted and nothing a venue wrote dropped; or different, and both stay. Either way it's ruled and never asked again. Same screen takes a profile down everywhere and puts it back. Tried on a real pair from the Rixey data: "Yiselle Santos" typed on a booking, "Yisell Santos" on the network. Raised from the unplaced page, merged from the staff page, the booking and the alias on the survivor afterwards.
- Extended the couple route for the shapes the real pages use: whole-list saves (PUT, one request instead of the browser's clear-then-insert), cascades on delete (budget payments, tag assignments), a per-resource conflict target (several singletons are unique on `(venue_id, wedding_id)`, not `wedding_id`), tables with no `venue_id`, and authorship set from the session rather than sent by the client. 24 resources configured, 39 tests. Migrated ceremony-chairs, rsvp-settings and guest-care. `guest_tag_assignments` got its own endpoint, since it carries no scope columns and is reached through the guest.

- Linked the 19 confident matches on Rixey's unplaced bookings through the real screen, one click each: Serendipity, Good Earth Flowers, Pampas Fox, Gateau Warrenton, four spellings of Chariots for Hire, two of ImTheDJ, Sarah Dodson to Sara Dodson, All Pro Charter to All Pro Charters, and so on. 35 unplaced remain: 4 maybes for a person, 7 that aren't names ("Vendor", "Hair"), 24 with nothing close on the network.
- Deployed the vendor network to Vercel under The Bloom House as `vendor-bloom`: https://vendor-bloom.vercel.app, pushes to `main` deploy. Production env has the Supabase keys, the directory key and the app URL, and no Resend key on purpose, so nothing can email a vendor; links can still be copied. Phil can sign in with the test login; `scripts/add-venue-staff.mts` makes him his own when there's an email to use.
- Fixed all five couple saves that could not work. Two were the page inventing a column name and three were features built ahead of their schema, and the direction was decided by what the live data and the other readers already use rather than by which side was written first. `allergies` wrote `allergy` where the column is `allergy_type`, and its reads used the same wrong name, so the list had been rendering a blank line for every allergy a couple entered. `bar_recipes` was worse: the page AND the extraction service both wrote name/servings_per_batch/notes/sort_order, and the extractor's comment gave as its reason that the page read those. The print view, the CSV importer and the three seeded rows have used cocktail_name/ingredients/instructions/servings all along. Migrations 420 (rehearsal, 34 columns), 421 (bar planner, 5) and 422 (nine more across allergies, checklist, decor and guests) — **none applied yet**.
- Migrated seven pages onto the route: ceremony-chairs, rsvp-settings, guest-care, beauty, final-review, staffing, worksheets. `wedding_config` now saves through it too.
- Traced why Rixey shows as not having a full free account, and it was not permissions. `billing-state.ts` decides a venue is on trial purely from `stripe_subscription_id IS NULL`, so Rixey (no Stripe sub, `trial_ends_at` 4 May 2026) read as a trial that expired four and a half months ago: the banner on every platform page, inquiry capacity capped to the `pre_opening` tier despite `plan_tier` being enterprise, and **autonomous sending switched off** through `isTrialExpiredNoSub`. Migration `423_billing_exempt.sql` adds the column, guards the billing columns against being set from the browser, and marks Rixey exempt. Eight tests. `require-plan.ts` was never the problem: it only restricts on past_due and canceled, and Rixey is null on both.
- Ran all four pending migrations. 420, 421 and 422 on both the main project (`jsxxgwprxuqgcauzlxcb`) and the test one (`ciwqxwohczzthvzqqgjx`); 423 on main only. Verified by probing every column afterwards: 9/9 on test, 13/13 on main. Rixey Manor now reads `billing_exempt: true` on `plan_tier: enterprise`.
- Fixed the login outage on `bloom-house-iota.vercel.app`. Isadora's console showed a CORS failure calling `https://jsxxgwprxuqgcauzlxcb.supabase.co/n/auth/v1/token` — that `/n/` is a literal backslash-n on the end of `NEXT_PUBLIC_SUPABASE_URL`. **Eight production values carried it**, in Doppler `bloom-house/prd` AND on Vercel: ANTHROPIC_API_KEY, EMAIL_FROM, NEXTAUTH_SECRET, NEXT_PUBLIC_DEMO_MODE, NEXT_PUBLIC_SUPABASE_URL, NOAA_CDO_TOKEN, RESEND_API_KEY, SERPAPI_API_KEY. So it was not only login: every AI call and all outbound email on that deployment were failing too, and the From header was malformed. Fixed in Doppler, redeployed, verified end to end.

## Found
- The unplaced page took 20 seconds. Two causes: a 227-option dropdown on every one of 55 cards, and the matcher re-keying all 77 ruled names for every one of 12,000 pairs. One shared datalist and keys cached per rulings list took the matching from 5.5 seconds to 0.3, same answers. Pages on production now come back in two to three seconds.
- Rixey's `is_local` and `is_budget_friendly` flags have nowhere to go on the network, so 33 and 34 vendors lost them. Reported by the import rather than dropped quietly; a decision for later (a venue-level "local" is really "near this venue", which is a distance question the schema already has columns for).
- Adding a vendor by another spelling of a name already on the venue's list was rewriting the venue's row from the add form, so "sammys rental inc." switched Sammy's off "recommended" and blanked the couple line. Caught because the directory came back without Sammy's. It only adds now.
- Resend refuses to send from `hello@thebloomhouse.ai` because the domain is not verified on the Resend account. The app copes (the link is shown to send by hand and the failure is recorded), but ContractHouse sends from the same address, so its emails will be failing the same way. Needs the domain verified at resend.com/domains.
- The vendor Supabase project (`zalhitfwrgiycginiuvl`) is an older one and the CLI's `db query` can't reach it: `permission denied to alter role cli_login_postgres`. The API keys don't do DDL either. The direct Postgres connection string works; it lives in `vendor_bloom/.env.local` as `SUPABASE_DB_URL`, gitignored.
- Creating a vendor from a booking recorded the booking's contact text twice (once on create, once on link), so a single sighting showed "seen 2 times". Fixed; the link step is the one that records it.
- The builder never knew the timeline or accommodations counts, which two of the section guards fall back on, so it now loads both. Not-yet-loaded is passed through as "cannot tell" rather than zero, because saying a couple's schedule is empty when nothing looked at it is the same lie in a new place.
- My first pass at the dress-code preset list was a guess and it was wrong: I had `formal`, `semi_formal`, `garden_party`; the site has `black_tie_optional`, `garden`, `smart_casual`. A test now pins the two lists together, because a missing key makes the panel call a section empty that the site would happily render.
- Couple-facing writes in Bloom mostly bypass the server: 25 pages under `_couple-pages` write straight from the browser with the anon client, 77 write calls, the website builder's `saveSettings` among them. That is why `logActivity` has 4 call sites against 126 route files that mutate, and why a Bloom couple's website toggle is exactly as untraceable as the Rixey one was. Not something to patch; it needs the writes to move behind API routes.
- Checked the RLS on the couple portal while I was in there and it is fine. Anon reads on `weddings` reach 60 of 85 rows but all four venues involved are `is_demo = true`; anon cannot update a `weddings` row it cannot read; anon insert on `wedding_website_settings` is refused. One loose end: migration 027's `anon_update_wedding_website_settings USING (true)` has no DROP anywhere in the history, and the table holds one row, so there was nothing hidden to test its update policy against. Worth the same probe once a real couple has built a site.
- 36 tests on the new config, and they earned it twice: the config claimed a `notes` column on `guest_list` that does not exist, and my own schema-reading helper used `[a-z_]+` so it silently skipped every column with a digit in it (`partner1_name`, `partner2_name`). Both would have shipped. A wrong name column is the failure worth guarding, because it does not throw, it just makes every feed entry read "added an item" for ever.
- The audit could not see its own subject at first. `/api/couple/[resource]` writes `.from(resource.table)`, a variable, so the literal scan found no tables and dropped the route off the report entirely. It now detects a runtime-chosen table and says so; that immediately surfaced `portal/config/copy`, which had been invisible for the same reason.
- `portal/config/copy` takes a table name from the request body. It is properly guarded (an explicit allowlist, plus a venue-access check on the source), so not a hole, but it is worth knowing it exists.
- **Five couple-facing saves cannot work, and never have.** Checked against the live schema, not the generated types:
  - `rehearsal/page.tsx` builds a payload of 41 fields of which **34 are not columns**. The table was created in migration 009 with nine and nothing ever added the rest. Every save that page has attempted has failed. Migration `420_rehearsal_dinner_columns.sql` is written and NOT applied.
  - `bar/page.tsx:888` adding a recipe writes `name`, `servings_per_batch`, `notes`, `sort_order`; the table has `cocktail_name`, `ingredients`, `instructions`, `servings`, `scaling_factor`.
  - `bar/page.tsx:741` the calculator save writes `event_duration_hours`, `notes_calculator`, `notes_list`, `notes_recipes`.
  - `bar/page.tsx:837` writes `from_calculator`.
  - `allergies/page.tsx:276` writes `allergy`; the column is `allergy_type`.
- Three of my own wrong numbers this session came from scanning a fixed number of characters after a `.from()` and running past the end of the statement. The first pass at the phantom-column check said 28 sites across five pages; constrained properly, and checked against the live database, it is 4 inline sites plus rehearsal. The false positive was a `.select()` on `guest_list` paired with a later insert into `allergy_registry`.
- `staffing_assignments` and `onboarding_progress` both have **partial** unique indexes (`WHERE role = '_calculator'` and `WHERE step IS NULL`), and Postgres will not take a partial index as an `ON CONFLICT` target: it answers 42P10 and the save fails. I had configured both as singleton upserts, which would have broken them. Production has five onboarding rows for one wedding and three worksheet rows for another, which is what gave it away — `wedding_worksheets` is one row per section, not per wedding, and I had that wrong too. There is now a test that checks every singleton's conflict target against a real, non-partial constraint in the migrations.
- `scripts/check-write-columns.mjs` guards the phantom-column class from now on, reading the generated types so it runs in CI without a database. Ceiling 10 today, goes to 0 once 420-422 are applied and the types regenerated.
- The worksheets page inserts into `admin_notifications` straight from the browser, so a couple can write a notification to the venue. Not migrated, not urgent, but it should be the server's to send.
- PR #1's stated blocker is cleared: there are five venues on prod and **four are `is_demo = true`**, so Rixey is the only production venue and nothing else can freeze when 417/420 runs.
- 423 is written to be superseded cleanly by PR #1: same column definition with `IF NOT EXISTS`, and the trigger deliberately carries the name `trg_venue_freeze` so that branch's richer version replaces it by name with nothing left behind.
- **Migration number collision:** master now has `420_rehearsal_dinner_columns.sql` and branch `trial-freeze` has `420_trial_freeze.sql`. Mine landed second and is already on master, so trial-freeze needs renumbering to 424 before it merges.
- `venues_org_update` (058) lets any authenticated org member update their own venue row with no column restriction, so adding `billing_exempt` without a guard would have let an org admin grant themselves a free account. The trigger covers the billing columns as a set, which also closes the same hole on `trial_ends_at` and `stripe_subscription_id`.
- **Doppler syncs into Vercel.** Correcting the eight secrets in `bloom-house/prd` propagated to Vercel production on its own; I never touched a Vercel env var. `DOPPLER_TOKEN` is not set on Vercel, so the build does not pull from Doppler — the integration pushes into it. Doppler is the source of truth, and fixing Vercel directly would have been overwritten on the next sync.
- Only production was affected. Doppler `dev` (18) and `stg` (33) are clean, and so are Vercel preview (52) and development (19). One bad production push did it, in both stores.
- `NEXT_PUBLIC_*` is inlined at build time, so the corrected env needed a rebuild before login could work. The last production deploy was 17h old and today's 36 commits to master had NOT auto-deployed, so I redeployed that same commit rather than shipping today's work as a side effect.
- Proof, with the key the deployment actually serves: `/n/auth/v1/token` returns 404 with no CORS header, which is exactly what the browser reported; `/auth/v1/token` returns 400 `invalid_credentials` with a proper `access-control-allow-origin` for the origin. Login round-trips.
- My local `.env.production` held a **legacy** anon key, which Supabase has now disabled ("Legacy API keys are disabled"). The deployment correctly uses the new `sb_pu...` publishable key. The local file is stale, not the deployment.

## Open
- The vendor network's build order is done, steps 1 to 8, and it's deployed. Left: the Resend domain (parked, Isadora's call), sending Rixey's vendors their new links (not until she says), the 4 maybes on the unplaced page, then the November merge into Bloom. Contract upload was decided out: it stays in the portal (couple upload exists, coordinator upload is on the plan) and tags through the network matcher. Contract upload from the venue side is also still to come; the page lists contracts but nothing puts one there yet.
- Trial-freeze PR #1 still waits on: whether any production venue besides Rixey has no Stripe subscription, and Phil being told.
- ContractHouse still has no Stripe keys on Vercel.
- PR #2 (`fix/nullable-connection-flags`) waits on Phil: `gmail_connections.sync_enabled` and `zoom_connections.is_active` are nullable and read as "not false" everywhere a person sees them, while two cron scans asked for "is true". Nothing is null today, so it changes nothing now.
- Bloom's couple-side audit trail is the real gap left. Same question Rixey answered this week, but it cannot be answered the same way while the writes go browser-to-Postgres.
- 100 API routes still write no activity entry, and the ceiling in `check:governance` holds them there. `node scripts/audit-activity-logging.mjs --list --area <name>` picks off an area at a time; admin and agent are the bulk of it, and agent is Phil's.
- 24 of the 25 couple pages still write from the browser. The route and the client helper are in place, so each page is now an independent swap: `coupleCreate` / `coupleUpdate` / `coupleRemove` in place of `supabase.from(...)`, dropping `wedding_id` and `venue_id` from the payload. Guests is the big one at 19 write sites.
- 21 of 25 couple pages still write from the browser. The route now covers every shape they use, so each is a mechanical swap.
- **Migration 420 needs running before the rehearsal page will save anything.** It only adds nullable columns and one unique index, and there is nothing to de-duplicate first because every save so far failed.
- The four other broken saves need either a migration or the page corrected to the real column names. `allergies` is a rename (`allergy` → `allergy_type`) and safe to just fix; the three bar ones look like a form built ahead of its schema, same as rehearsal, so they want a decision on which way round to fix them.
- 14 couple pages still write from the browser. Six of them (rehearsal, bar, allergies, decor, guests, checklist) cannot finish until 420-422 are applied and the types regenerated, because the config test checks every field against the generated types. The other eight are mechanical.
- Two things I got wrong while doing this, both worth remembering: inserting an import after "the last line matching ^import" lands inside a multi-line `import { ... } from` block, and hand-rolled patch scripts kept matching nothing because the search string used 
 against CRLF files. The helper now normalises both and refuses to write unless every edit applied exactly once.
- **Migration 423 needs running on both projects** (`jsxxgwprxuqgcauzlxcb` and `ciwqxwohczzthvzqqgjx`) before Rixey gets full access. Nothing changes until it does.
- master is level with consolidation again (`502c0fd1`, 35 commits fast-forwarded).
- `.env.production.bak-before-newline-fix` is a local backup I made before stripping the newlines. It holds production secrets and is gitignored; delete it when you are happy.
- Today's 36 commits on master are still not deployed to production. Master pushes do not appear to auto-deploy this project; the last production build was 17h old before I redeployed it. Worth deciding whether that is intentional.
- The local `.env.production` anon key is a disabled legacy one. Worth refreshing from Doppler rather than trusting that file.
