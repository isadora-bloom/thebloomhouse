# 2026-09-17 · Isadora

## Done
- Started the work diary (`diary/`). One file each per day so Phil's PRs and my branch never conflict over it. The rule for Claude sits in `CLAUDE.md`, so Phil's sessions will pick it up once he's pulled `consolidation`.
- Backfilled yesterday's entry from session notes.

## Decided
- Split the work with Phil so we're not in the same code.
- Phil takes the Agent and Intel.
- I take, roughly in this order: an admin panel, getting ContractHouse ready to merge, starting to road test the client portal, then going page by page through the UIX as a venue would see it. After that, maybe more on the vendor network.
- If something in my areas needs an Agent or Intel change, it goes to Phil rather than getting done quietly on my side.
- Put the Phil/Isadora split at the top of `CLAUDE.md` too, so it doesn't scroll out of the diary.
- Added a startup hook that loads the last three days of diary entries into every Claude session in the repo, rather than just asking Claude to go and read them.

## Found
- There are now two contract systems. ContractHouse is its own app (about 11.5k lines, own Supabase, live for Rixey, untouched since 29 June). Bloom also grew native contracts on 14 Sep (W57, migration 409), a deliberately smaller version with no payments and no clause library. Getting ContractHouse ready to merge first needs a decision on which one survives.
- What's under `/admin` today is mostly identity and attribution tooling, which sits closer to Phil's side than to a venue admin panel.
