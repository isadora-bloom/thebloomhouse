# 2026-09-17 · Isadora

## Done
- Started the work diary (`diary/`). One file each per day so Phil's PRs and my branch never conflict over it. The rule for Claude sits in `CLAUDE.md`, so Phil's sessions will pick it up once he's pulled `consolidation`.
- Backfilled yesterday's entry from session notes.
- Closed two of the leftovers from July's couple-portal parity audit. Sage can now see the inspiration board, and the couple's vendor list has the filters and badges the Rixey one has.
- Sage reads the board as words, not pictures. The wedding-context block now carries how many images they have saved, the tags they use most with counts, and up to twelve of their newest captions, wrapped in the untrusted-content envelope because a caption is text a couple typed. It carries a rail with it: Sage has read their notes and has not seen the photos, so it must not describe or praise an image, and must say so if asked. Couple-chat prompt goes to v2.2 with a changelog row. Before this, someone could spend an evening captioning a board of candlelit tables and still get a generic answer about lighting.
- The vendor page was worse than "no filters". Live, `vendor_recommendations` held `photography` next to `photographer`, `florals` next to `florist`, `catering` next to `caterer`, plus `cake`, `music`, `videography` and `bartender`. The couple page looked labels up by exact string, so thirteen of the twenty-eight rows showed to couples as "Other", sitting in an "Other" pill beside the correctly-labelled one. The staff editor was making it worse, writing Title Case that matched nothing. So the vocabulary now lives in one file both pages read, `src/lib/vendors/vendor-types.ts`, and an unrecognised spelling gets humanised from its own text instead of being collapsed into "Other".
- On top of that: search, the four Rixey toggles (offer, local, budget-friendly, has worked here before), matching badges on the card, a "9 of 28 vendors" line, and a clear-filters panel. An over-narrow filter used to tell a couple their venue had not added any vendors yet.
- Migration 417 adds the three flag columns and folds the existing spellings. Checkboxes for them sit in the staff vendor modal, which also now shows a save error instead of leaving the modal open saying nothing.

## Decided
- Split the work with Phil so we're not in the same code.
- Phil takes the Agent and Intel.
- I take, roughly in this order: an admin panel, getting ContractHouse ready to merge, starting to road test the client portal, then going page by page through the UIX as a venue would see it. After that, maybe more on the vendor network.
- If something in my areas needs an Agent or Intel change, it goes to Phil rather than getting done quietly on my side.
- Put the Phil/Isadora split at the top of `CLAUDE.md` too, so it doesn't scroll out of the diary.
- Added a startup hook that loads the last three days of diary entries into every Claude session in the repo, rather than just asking Claude to go and read them.

## Found
- There are now two contract systems. ContractHouse is its own app (about 11.5k lines, own Supabase, live for Rixey, untouched since 29 June). Bloom also grew native contracts on 14 Sep (W57, migration 409), a deliberately smaller version with no payments and no clause library. Getting ContractHouse ready to merge first needs a decision on which one survives.
- What's under `/admin` today is mostly identity and attribution tooling, which sits closer to Phil's side than to a venue admin panel.
- Pinned down what my five areas mean. The admin panel is for Bloom the business (registrations, billing, usage). Bloom's own native contracts (W57) get deleted and ContractHouse gets solidified and merged in instead, since it's much further along. The portal road test runs on historic Rixey portal data with identifying details changed, walked through as venue and as couple. The UIX pass will be a spreadsheet of every page, coming back to that soon. The vendor network starts by seeing what we can take from the Rixey portal. ContractHouse and vendors both merge into Bloom once solid.
- Wrote `ISADORA-PLAN.md`, the order for my five areas up to 8 Nov. Delete W57 first, read the Rixey portal once for both test data and vendors, load an anonymised venue, then walk it from both ends. Admin panel and ContractHouse run alongside. Merges go last.
- Everything merges into Bloom by 8 Nov, ContractHouse and the vendor network both. The vendor network gets built standalone first, from the January schema already sitting in the `vendor_bloom` repo. The admin panel is for everyone at Bloom. The test venue gets 3 or 4 anonymised Rixey couples. Real clients move onto Bloom from 2027 weddings onwards. Plan updated so the vendor build starts in week 2 rather than week 5.
- Side-by-side audit of the vendor network: the January `vendor_bloom` schema has the right shape for 200 venues (one vendor, many venues) and the Rixey portal has the machinery real data needs (aliases, merges, evidence, self-edit links). Build the first with the second inside it. `audits/2026-09-17-vendor-network-side-by-side.md`.
- Rixey portal vs Bloom portal again, since July. Couple side is close. Venue side, the Rixey portal has pulled ahead: walkthrough notes, family contacts, meeting recording, sheet and document import, guest import. `audits/2026-09-17-rixey-portal-vs-bloom-portal.md`.
- Admin panel plan written for 200 venues, `ADMIN-PANEL-PLAN.md`. Biggest finding: Bloom staff and venue users are the same kind of user today (`super_admin` sits in `user_profiles`), which has to split before anything else. Also the pricing page says no trial while the code gives every venue 14 days.
- Admin panel answers: the 14-day trial stays (the pricing page is what's wrong), billing through Stripe, alerts built into Bloom rather than Slack, a notice is enough when staff view a venue. Enterprise invoicing and what makes a venue "red" still open.
- Started the vendor network properly in `vendor_bloom`: `DESIGN.md` and the first migration (one shared vendor per business, each venue's view private, Rixey's aliases, merges, evidence and edit links built in). The SQL parses but hasn't run, because the repo has no Supabase project yet. Defaulted four privacy questions (vendors don't see who recommends them, ratings stay private, Bloom staff answer merge questions, blocked is invisible to the vendor).

## Decided (evening)
- Test data is real Rixey data for the 3 or 4 most complete couples, with only emails and phones swapped so no real person gets a notification. Internal only.
- The Rixey portal is done with new features, fixes only. Its walkthrough notes, family and contacts, meeting recording and document import get built into Bloom. The Sheet sync goes. Venue staff can edit a couple's sections for them.
- Rixey isn't sending contracts through ContractHouse today, so there are no live contracts to protect or move.
- Admin panel: one Stripe subscription per organisation, Enterprise through Stripe too, Isadora and Phil as Founder, the "red venue" rule agreed, and a new screen for opt-ins plus a place for venues to suggest them.
- Vendors: privacy defaults confirmed, other venues can see vendor contact details, invites wait for the network. All 52 Rixey merge questions were already answered on 26 Aug.
- Pricing page to mention the 14-day trial. W57 contracts to be deleted.
- Deleted Bloom's own contracts (W57): the generate, send, sign and status code, the signing page, the contract template settings page and nav entry, the fifth section on the wedding page, the signing pill in the couple's contract library, and the demo contract seed. Prod had 21 generated contracts, all seeded rows on the four demo venues, none real. The library and couple contracts route now skip any leftover generated rows. The E2E seed gives journey 27's chat an ordinary uploaded contract instead of a signing link, and the signing tests in sections 27 and 32 are gone. The 409 columns stay in the database for now.
- Checks: source type-checks clean (only Next's stale generated types still name the deleted pages), tests near the change pass, internal links OK (253 pages, 415 API routes), every governance guard green except the migration file count (412 against a budget of 409), which was already over from 414 to 416 before today. E2E sections 27 and 32 not rerun.
- Pricing page now says there's a 14-day free trial with no card, in a note under the heading and a new question in the pricing FAQ (thebloomhouse-website `91ae2ac`). After the 14 days only auto-send stops, drafts carry on.
- Claude now brings up open pull requests at the start of every session, for Phil and me, so a PR from one of us doesn't sit unseen. The first is #1, the trial freeze, waiting on Phil because it touches the AI client and email sending.
- A timezone bug in the special-offer badge, caught by a test I wrote for my own helper. `offer_expires_at` is a date column, and the page was doing `new Date(expiry) >= new Date()`, which parses a bare date as midnight UTC and compares it against the moment of rendering. The offer went dark part-way through its last day, or missed the day entirely, depending which side of UTC the couple was sitting on. It compares calendar dates as strings now.
- The same parity audit's remaining items are still open and untouched: the email half of cross-party notifications (`couple-notifications.ts` is 36 lines with no email path at all), per-night Fri/Sat room assignment, the resources course link, and the borrow/venue-inventory half of the Sage context work. Rixey's two cuisine flags on caterers were left out on purpose, since no Bloom venue has that data and I did not want to invent a column for it.
- Audited ContractHouse, read-only, written up in `contracthouse/AUDIT-2026-09-17.md`. It type-checks and builds, and the pieces are there, but couples currently see raw `{{placeholders}}` in their contracts (eleven variable names never get a value), the venue-side audit trail has never written a row because of the RLS policies, the sign and done pages promise emails nothing sends, and production has no Stripe keys. Co-signer and the certificate of completion were never built. Production only holds test contracts, so the fixes can be bold. Fix order is in the file.
- Fixed everything on the ContractHouse audit list, on master (`9b38a54`, `72c5a07`). Couples no longer see raw placeholders: venue settings feed the clauses, and a draft with a blank can't be sent. Signing now sends the couple their signed PDF and tells the venue. Partner 2 can co-sign. There's a certificate of completion. Milestones go overdue on their own and reminders say how to pay. Card payments are taken by Bloom and sent on to the venue's Stripe account by hand from the Payments page, which is what Isadora asked for; Stripe keys still aren't on Vercel. Migration 016 is on the database. Fourteen tests, lint and CI added, Next upgraded.
- Screenshotted every ContractHouse page at desktop and phone width with Playwright (the Chrome extension wasn't connected). The venue app had no navigation at all on a phone; it has a menu now. The couple page got a sticky sign bar, the dashboard got a "what needs doing" strip, and the grey photo placeholders are gone. Findings and what's left are at the end of `contracthouse/AUDIT-2026-09-17.md`.
- Went back over ContractHouse for whether the pages explain themselves. Added a Clauses page (there was none, and the import review linked to it), a strip on each contract showing Draft, Sent, Signed, Paying, Done with what happens next, plain intros on Templates, Contracts and Packages & pricing, and a hint under every item in the phone menu. Nav now says "Import a contract" and "Packages & pricing" rather than "Onboarding" and "Options".
- Compared ContractHouse with HoneyBook, Dubsado, 17hats, Táve, Aisle Planner, Planning Pod, Rock Paper Coin, DocuSign, Dropbox Sign, PandaDoc, Ironclad, Common Paper and Juro: structure, what they promise, what they don't. Written up in `contracthouse/COMPARISON-2026-09-17.md`. Nobody in weddings explains clauses to the couple; the signing tools set the bar on proof and ContractHouse's new certificate meets it. Gaps to close: no "not a law firm" line anywhere, no reminder to sign before the hold ends, no declined state, no receipts on the couple link, clause explanations edited in place with no history.
- Built the seven gaps from the comparison (`contracthouse` `40d5247`, migration 017): the "not a law firm" line for venue and couple, hold-ending and hold-ended emails, a declined state so a couple can walk away cleanly, the deal on one screen at the top of the couple's page with a jump list, clause edit history, receipts and a receipt email per payment, and a stated payout time.
- Compared the couple portal with Planning Pod, Tripleseat, Weven (now absorbed by The Knot), Seated With Love, Aisle Planner, HoneyBook, Zola, The Knot, Joy, Bridebook, Prismm, Timeline Genius and Losava: `audits/2026-09-17-portal-market-comparison.md`. Bloom has more sections than any of them; what it lacks is the small mechanisms that make a portal feel finished. Thirteen ranked, starting with a "needs you" strip on the couple's home, a venue lock date before the wedding, three-level section permissions, a checklist that counts down from the date, and live venue documents. Nothing built; it feeds the road test and the Rixey-features-into-Bloom step.

## Done (later, from the parity audit)
- Went back through the September Rixey-vs-Bloom audit and closed the couple-side rows that were plain bugs rather than missing features. Guest CSV import now matches on email, or on first and last name when there is no email, and updates the guest instead of adding a second one. Only the columns in the file get written, so an RSVP already on file survives a re-import. The preview says how many rows will update, and the page reports added, updated, unchanged and failed counts afterwards, where before it finished in silence. The matching lives in `src/lib/services/couple/guest-import.ts` with 24 tests.
- Inspiration board: either partner could delete the other's pins, and the live delete policy was venue-wide, so a couple user could in principle delete another couple's pin at the same venue. `uploaded_by` has existed since migration 004 and was never written. The page stamps it now, hides the X on pins that aren't yours, and migration 419 tightens the policy (venue staff keep venue scope, a couple is held to their own wedding and their own pins, unauthored rows stay deletable so the 102 existing images aren't stranded). 419 is live on the E2E project, not on production yet.
- Day-of memories: the venue's arrange buttons wrote `sort_order` but neither page ever read it, so reordering did nothing. Both sides order by it now. Uploads take a whole selection at a time, and one bad file no longer loses the batch: each file stands on its own and the failures are named at the end.
- Worksheets "send to team" wrote a notification at normal priority, and the bell only shows high and urgent, so it reached nobody unless a coordinator opened the full notifications page. It is high now.
- The session doing this closed part way through, so the batch was checked and committed afterwards: type-check clean, tests pass. The sidebar's collapse-state memory (browser storage, restored after mount to avoid a hydration mismatch) went in with the portal-calm batch instead, since that batch owns the sidebar file.
- Housekeeping: the trial-freeze branch and `consolidation` both have a migration numbered 417 (trial freeze vs vendor attributes). The trial-freeze one needs renumbering before PR #1 merges.
