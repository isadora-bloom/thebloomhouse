# 2026-09-16 · Isadora

## Done
- Older E2E sections repaired against the built bundle and pushed as `0a7b96bc` on `consolidation` (46 files).
- Found and fixed that demo-exit had never worked.
- Pipeline was skipping the spine for targets it couldn't send to. Fixed.
- Sage's warmth scoring now reads off the spine. Prompt bumped to v1.7.
- Built an audit ZIP from the working tree before committing (`Downloads/bloom-house-audit-2026-09-16T1346.zip`).

## Decided
- Phil gets his own branch off `consolidation`, works through PRs, and uses the test Supabase project only. First task for him: seed, then get the seven journeys green locally.

## Open
- Rotate the Google OAuth client secret.
- Mobile "Exit demo" X is covered by its own banner at 375px, and `/today` overflows sideways.
- Rerun E2E sections 11 and 20, the spec and helper changes haven't been run yet.
- Migrations 414, 415 and 416 aren't on prod.
- Test project Hawthorne still has `sarah.measure@example.com` rows left over from measurement scripts.
- `[knowledge-gaps] insert failed: null value in column "category"` showed up in the run-3 log. Not looked at.
- CI only runs on `master`. Needs `consolidation` added before Phil opens PRs.
